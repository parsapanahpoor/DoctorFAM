@model DoctorFAM.Domain.ViewModels.Tourist.Token.AddPhoneNumbersViewModel
@using DoctorFAM.Application.Convertors
@using DoctorFAM.Domain.Enums.DoctorReservation

@{
    ViewData["Title"] = @Localizer["افزودن شماره تلفن"];
}

@section Scripts
    {
    <partial name="_JqueryValidation" />
}

@section Breadcrumb
    {
    <li>
        <a asp-action="ManagePage" asp-controller="Token" asp-area="Tourist"> مدیریت توکن ها </a>
    </li>
}

<div class="row">
    <div class="col-lg-12 col-sm-12">

        <div class="widget">
            <div class="widget-header bordered-bottom bordered-green">
                <span class="widget-caption"> @Localizer["افزودن اطلاعات مسافر"] </span>
            </div>
            <div class="widget-body">
                <div>

                    <form asp-action="AddReservationDateView" asp-controller="Appointment" asp-area="Doctor" method="post">
                        <div class="modal-body">
                            <div class="row">

                                <div class="form-group col-md-6 col-sm-12">
                                    <label asp-for="PhoneNumber">@Localizer["شماره موبایل"]</label>
                                    <span style="color:red;">*</span>
                                    <input type="text" class="form-control" asp-for="PhoneNumber">
                                    <span class="text-danger" asp-validation-for="PhoneNumber"></span>
                                </div>

                                <div class="form-group col-md-6 col-sm-12">
                                    <label asp-for="RequiredAmount">@Localizer["تعداد توکن مورد نیاز"]</label>
                                    <span style="color:red;">*</span>
                                    <input type="number" class="form-control" asp-for="RequiredAmount">
                                    <span class="text-danger" asp-validation-for="RequiredAmount"></span>
                                </div>

                                <div class="clearfix"></div>

                                <div class="form-group col-md-12">
                                    <button type="submit" class="btn btn-success">@SharedLocalizer["ثبت"]</button>
                                </div>

                            </div>
                        </div>

                    </form>

                </div>
            </div>
        </div>
    </div>
</div>


@*@if (reserve != null && reserve.Any())
{
    <div class="row">
        <div class="col-lg-12 col-sm-12">

            <div class="widget">
                <div class="widget-header bordered-bottom bordered-green">
                    <span class="widget-caption"> @Localizer["List Of Your Reservation Dates"] </span>
                </div>
                <div class="widget-body">
                    <div>
                        <div class="modal-body">
                            <div class="row">

                                @foreach (var item in reserve)
                                {
                                    <div class="form-group col-md-2 col-sm-12">
                                        <div class="form-control">
                                            <a asp-action="ReservationDateDetail" asp-controller="Appointment" asp-area="Doctor" asp-route-ReservationDateId="@item.Id">
                                                @item.ReservationDate.ToShamsi()
                                            </a>
                                        </div>
                                    </div>
                                }

                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>

}*@