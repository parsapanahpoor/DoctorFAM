@model bool

@if (Model == true)
{
     <li>
            <a href="#" class="menu-dropdown">
                <i class="menu-icon fa fa-bell"></i>
                <span class="menu-text"> @SharedLocalizer["مدیریت تبلیغات"]</span>

                <i class="menu-expand"></i>
            </a>

            <ul class="submenu">
                <li>
                <a asp-area="UserPanel" asp-controller="CustomerAdvertisement" asp-action="ListOfAdvertisements">
                        <span class="menu-text">@SharedLocalizer["لیست تبلیغات"]</span>
                    </a>
                </li>
            </ul>
        </li>
}
else
{

}